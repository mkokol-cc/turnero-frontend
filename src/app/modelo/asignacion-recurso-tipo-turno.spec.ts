import { AsignacionRecursoTipoTurno } from './asignacion-recurso-tipo-turno';

describe('AsignacionRecursoTipoTurno', () => {
  it('should create an instance', () => {
    expect(new AsignacionRecursoTipoTurno()).toBeTruthy();
  });
});
